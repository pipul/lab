>>文件管理
	—— 删除，创建，复制 rm/touch/cp
	—— 重命名，移动 mv
	—— 压缩，解压 zip/unzip
	—— 设置权限 chmod
	—— 更改文件用户和组，支持递归 chown/chgrp
	—— 列目录 ll/ls
	—— 切换目录 cd
	—— 编辑文件 vim
	—— 远程主机传输到本地，本地上传至远程主机 get/put
	—— 远程下载 down
	
>> 账户管理
	—— 服务器信息
	—— 更改密码
	
>> 域名管理
	—— 添加新域名（支持顶级，一级，二级，三级）
